{
  "name": "outperform-it-website",
  "version": "1.0.0",
  "description": "Outperform IT Solutions — Production website",
  "scripts": {
    "dev":   "tailwindcss -i styles.css -o dist/styles.css --watch",
    "build": "tailwindcss -i styles.css -o dist/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
